here's a self-signed certificate and its private key.
not terribly valuable, but useful for local testing.

the "tool" folder has a perl script that will update 
MozillaRootCertificates.as with the latest certificate
set from Mozilla.
